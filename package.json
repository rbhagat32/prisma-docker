{
  "scripts": {
    "dev": "docker compose -f compose.dev.yaml up --watch",
    "prod": "docker compose -f compose.prod.yaml up -d --build",
    "clean": "docker compose -f compose.dev.yaml down --rmi local --remove-orphans && docker image prune -f"
  }
}
